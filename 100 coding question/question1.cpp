// C++ Program to check whether a number is positive or negative

// For Instance,
// Input : num = 10
// Output :The number is Positive

#include <stdio.h>
#include <iostream>

using namespace std;
int main()
{
    int num = -30;
    if (num < 0)
    {
        cout << "The number is pr";
    }
    else
    {
        cout << "The Number is postitive";
    }

    return 0;
}